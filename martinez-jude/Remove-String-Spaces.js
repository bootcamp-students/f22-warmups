function noSpace(x){
let y = x.split(" ").join("");
return y;
}

// y is equal to x string apply method .split and .join
// .split splits a string into two substrings at string value ' ' of y string
// .join combines the two substrings with '' value inbetween
