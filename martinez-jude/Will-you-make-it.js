const zeroFuel = (distanceToPump, mpg, fuelLeft) => {
  // TODO\
  return (fuelLeft*mpg >= distanceToPump);
  };
//   if (fuelLeft*mpg >= distanceToPump){
//     return true;
//   } else {
//     return false;
//   } 
// };

// Func zeroFuel 3 perims distanceToPump, mpg, and fuelLeft
// return short hand if statement
// condition 
// if true will return true 
// if false will return false
