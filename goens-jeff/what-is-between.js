// Pseudocode
// CREATE blank array
// CREATE for loop
// PUSH to array

function between(a, b) {
  let results = [];
  for (let i = a; i <= b; i++) {
    results.push(i);
    
    }
   return results;
  }
