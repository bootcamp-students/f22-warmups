function arrayPlusArray(arr1, arr2) {
    //declare arr1 as an object
    const firstArray = arr1;
    //declare arr2 as an object
    const secondArray = arr2;
    //loop through the index of arr1
    for (num of firstArray) {
        let 

    }
        //for every intiger in index, add to the last
        //store in a variable named?
    //loop through the index of arr2
        //for every intiger in index, add to the last
        //store in a variable named?
    //add both numbers together
    //return sum
  }