// CREATE function zeroFuel that holds 3 ints
const zeroFuel = (distanceToPump, mpg, fuelLeft) => {
  // TODO

// if distancetopump <= distanceToPump * mpg = true
 return distanceToPump <= (mpg*fuelLeft)  
//else False
}
