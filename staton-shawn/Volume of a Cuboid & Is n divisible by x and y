Volume of a Cuboid

class Kata {
  static getVolumeOfCuboid(l, w, h) {
    return l * w * h;
  }
}


Is n divisible by x and y

 function isDivisible(n, x, y) {
   if (n%x === 0 && n%y === 0) {
     return true;
   } else {
     return false;
   }
}
