//loop through array check if index does not equal index - 1
//set empty var
//concat to var
function findUniq(arr) {
let final = []
 for (let i = 0; i < arr.length; i++) {
   if (arr.lastIndexOf(arr[i]) !== arr.indexOf(arr[i])) {
    continue;
   }
   final.push(arr[i])
 } 
return final[0];
}
