def kill_monsters(health, monsters, damage):
    hits = (monsters -1)//3
    damage *= hits
    health -= damage
    return ("hits: " + str(hits) + ", damage: " + str(damage) + ", health: " + str(health)) if health >0 else 'hero died'
