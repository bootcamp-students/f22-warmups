/*
FUNCTION getSum (a,b)
sum = a + b
    IF (a === b)
        return a;
    ELSE
        return sum
*/
function getSum( a,b ) {
  let sum = a + b;
    if (a === b) 
      return a;
    else
      return sum;
}