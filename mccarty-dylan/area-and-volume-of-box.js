//calculate for formula of area
//calculate for formula of volume 
//return values

function getSize(h, w, d) {
    let volume = h * w * d;
    let area = (2 * (l * w) + (l * h) + (h * w))

    return [volume, area]
}