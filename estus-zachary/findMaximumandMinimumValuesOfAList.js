// Math libary has built in functions to find the min and max

var min = function(list){
    let min = Math.min(...list);
    return min;
}

var max = function(list){
    let max = Math.max(...list);
    return max;
}
