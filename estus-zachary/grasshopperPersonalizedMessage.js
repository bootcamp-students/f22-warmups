// Check if name is the same as owner
// Display correct message depending on the boolean
function greet (name, owner) {
  if(name === owner)
    return "Hello boss";
  else
    return "Hello guest";
}
