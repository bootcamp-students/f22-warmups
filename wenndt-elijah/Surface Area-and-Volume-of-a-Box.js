function getSize(width, height, depth) {
//pseudocode
//get length
//get width
//get height
//use area formula to calculate area
//use volume formula to calculate volume

area = (2 * depth * width) + (2 * depth * height) + (2 * width * height);
volume = (depth * width * height);
return [area, volume];
  }
