//get length width height
//GET formula for volume of a cuboid
//RETURN volume

class Kata {
  static getVolumeOfCuboid(length, width, height) {
    let volume = length*width*height;
    
    return volume
  }
}
