function isDivisible(n, x, y) {
    //FUNCITON
    console.log(n);
    return ((n % x)==0 && (n % y)== 0);
    //IF N is divisible by X  and N is divisible by X
        //THEN return TRUE
    //ELSE return FALSE
    //ENDFUNCTION
  }
