function filter_list(l) {
  // Return a new array with the strings filtred out
 return l.filter(list => typeof list == "number");
}
// function isNumber

// given an array with strings and intergers (non negative)
// need to extract the intergers from the array and return the a new array with just the number
// using filter, we will check to see if the array value === number and return just those
