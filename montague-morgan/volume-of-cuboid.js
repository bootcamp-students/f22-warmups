class Kata {
  static getVolumeOfCuboid(length, width, height) {
    return (length*width*height)
  
  }
}
// need to get the volume of cuboid
// volume of a cuboid or box is length*width*heightheight
// return length*width*height
